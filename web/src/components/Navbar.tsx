import React from 'react';

/**
 * Navbar (Legacy wrapper)
 * App navigation is now handled by AppLayout (Left Sidebar + Header Bar + Mobile Bottom Nav).
 * Returns null to maintain backward compatibility for any page rendering <Navbar />.
 */
export const Navbar: React.FC = () => {
  return null;
};

export default Navbar;
